import styled from 'styled-components'

const EbacJobs = styled.header`
  background-color: var(--cor-secundaria);
  color: var(--cor-primaria);
  text-align: center;
  padding: 24px 0;
`
export default EbacJobs
